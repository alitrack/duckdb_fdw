-- complain if script is sourced in psql, rather than via ALTER EXTENSION
\echo Use "ALTER EXTENSION duckdb_fdw UPDATE TO '1.1.3'" to load this file. \quit

-- Add any specific migration steps or changes here
-- For minor version upgrades, this file might be empty if no structural changes are needed