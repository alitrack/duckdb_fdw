## 1. OpenSpec and maintenance docs

- [x] 1.1 Write proposal, design, tasks, and spec additions for developer-surface cleanup
- [x] 1.2 Add `CONTRIBUTING.md` and rewrite `CHANGELOG` into a structured format

## 2. Reproducibility and code cleanup

- [x] 2.1 Replace `strtok()` usage with a shared reentrant helper path
- [x] 2.2 Pin the default DuckDB bootstrap version while preserving explicit override behavior
- [x] 2.3 Remove unused maintenance residue from the build graph
